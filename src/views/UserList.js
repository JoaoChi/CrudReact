import React from "react";
import { View, Text, FlatList } from "react-native";

export default props => {
    return (
        <View>
            <FlatList
                data={ }
            />
        </View>
    )
}